/** Shared between PageLoader and Hero so the hero's entrance stagger kicks
 * off right as the loader finishes fading out, instead of playing underneath it. */
export const LOADER_DURATION_MS = 900;
export const LOADER_DURATION_S = LOADER_DURATION_MS / 1000;
